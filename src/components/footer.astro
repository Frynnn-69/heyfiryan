---
import { SOCIAL_LINKS } from "@/lib/constants";
---

<footer class="w-full border-t border-dashed md:border-b">
  <div
    class="mx-auto flex max-w-3xl flex-col-reverse items-center justify-between gap-4 border-dashed px-4 py-3 md:border-x lg:flex-row"
  >
    <p class="flex items-center gap-2 text-sm text-foreground/70">
      Built by <a
        href="https://github.com/Frynnn-69"
        class="font-medium text-primary underline underline-offset-4">Frynnn</a
      > <span class="flex items-center gap-1 font-medium text-foreground">
        •  Like what you see? Say hi 
        <span class="md:hidden">👋</span>
        <span class="hidden md:inline">👉</span>
      </span>
    </p>

    <div class="flex items-center justify-center gap-4">
      {
        SOCIAL_LINKS.map(link => (
          <a href={link.url} aria-label={link.name} class="inline-flex items-center">
            <link.icon className="size-4 duration-150 hover:text-primary" />
            <span class="sr-only">{link.name}</span>
          </a>
        ))
      }
    </div>
  </div>
</footer>
